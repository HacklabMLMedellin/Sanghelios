# Entorno Conda equivalente (el proyecto se gestiona con uv; ver README).
# Uso:  conda env create -f environment.yml && conda activate sanghelios
name: sanghelios
channels:
  - conda-forge
dependencies:
  - python=3.13
  - pip
  - pip:
      - -r requirements.txt
